name: deeppurpose-md-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11    # 🔧 Add this line to avoid Python 3.13
  - openmm
  - openmmforcefields
  - openff-toolkit
  - pdbfixer
  - rdkit
  - pip
  - pip:
      - meeko
      - vina
      - scipy
      - gemmi
      - biopython
      - pubchempy
      - transformers
      - torch
      - tqdm
      - matplotlib
      - pandas
      - pandas-flavor
      - subword-nmt
